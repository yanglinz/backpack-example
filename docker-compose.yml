# @backpack
# This is an autogenerated docker-compose file.
# It is not meant to be hand-edited.

version: "3.6"
services:
  example_server:
    build:
      context: .
      dockerfile: .backpack/docker/python-dev.Dockerfile
    command: bash -c ".backpack/runtime/entry-dev.sh"
    environment:
    - DJANGO_SETTINGS_MODULE=projects.core.settings
    - BACKPACK_DOCKER_COMPOSE=true
    ports:
    - 8000:8000
    - 8080:8080
    volumes:
    - ".:/app/"
    - /app/node_modules
    - $HOME/.config/gcloud:/home/app/.config/gcloud

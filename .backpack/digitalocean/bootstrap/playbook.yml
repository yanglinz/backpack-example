- name: DigitalOcean bootstrapper
  hosts: all
  tasks:
    - name: Empty task
      copy:
        content: Hello world!
        dest: /tmp/test.txt
    - name: Testing
      command: ls -la
      register: hello

    - debug: msg="{{ hello.stdout }}"
